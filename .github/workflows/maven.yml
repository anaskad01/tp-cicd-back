# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "*" ]

env:
  imagename: anaskad01/library:${{ github.ref_name }}

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml


    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ vars.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}


    - name: Build Docker image
      run: docker build -t ${{ env.imagename }} .


    - name: Push Docker image
      run: docker push ${{ env.imagename }}


#deploy:
#    if: github.ref_type == 'tag'
#    needs: build
#    runs-on: ubuntu-latest
#    steps:
      - name: Deploy Docker service on Render
        run: |
          echo "Triggering Docker deploy on Render..."
          curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_BACK }}

      - name: Deploy Frontend (Static Site) on Render
        run: |
          echo "Triggering Frontend deploy on Render..."
          curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_FRONT }}
